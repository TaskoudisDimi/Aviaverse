JWT_SECRET=supersecretkey_minimum32characters_abc
GEMINI_API_KEY=AIza...

# Resend (https://resend.com) — used to send password-reset emails.
# Leave blank to disable email sending (forgot-password requests will be
# accepted but no email will actually go out — logged as a warning).
RESEND_API_KEY=re_...
EMAIL_FROM=VJet-Academy <onboarding@resend.dev>
FRONTEND_URL=http://localhost:3000